<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1, shrink-to-fit=no"
    />
    <link
      rel="stylesheet"
      href="https://fonts.googleapis.com/css?family=Roboto:400,700"
    />
    <title>Register</title>
    <link
      rel="stylesheet"
      href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css"
    />
    <link
      rel="stylesheet"
      href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css"
    />
    <link rel="stylesheet" href="./style/style.css" />
    <script src="dist/pristine.js" type="text/javascript"></script>
  </head>
  <body>
    <div class="register-container">
      <div class="signup-form">
        <form action="/auth/register" id="regis-form" method="post">
          <h2>Register</h2>
          <p>Please fill in this form to create an account!</p>
          <hr />

          <div class="form-group">
            <div class="row">
              <div class="col form-group" id="flfield">
                <input
                  type="text"
                  class="form-control"
                  name="name"
                  placeholder="Name"
                  required
                  data-pristine-required-message="**Please enter  name"
                />
              </div>
            </div>
          </div>
          <div class="form-group">
            <input
              type="text"
              class="form-control"
              name="email"
              placeholder="Email"
              required="required"
              data-pristine-required-message="*Please enter email"
              data-pristine-pattern="/^[a-z0-9]+@[a-z]+\.[a-z]{2,3}/"
              data-pristine-pattern-message="*invalid email"
            />
          </div>
          
          <div class="form-group">
            <input
              type="password"
              class="form-control"
              name="password"
              placeholder="Password"
              required
              id="pwd"
              data-pristine-required-message="*Please choose a password"
              data-pristine-pattern="/^(?=.*?[A-Z])(?=.*?[a-z])(?=.*?[0-9]).{5,}$/"
              data-pristine-pattern-message="Minimum 5 characters, at least one uppercase letter, one lowercase letter and one number"
            />
          </div>
          <div class="form-group">
            <input
              type="password"
              class="form-control"
              name="confirm_password"
              placeholder="Confirm Password"
              required
              data-pristine-required-message="*please confirm password "
              data-pristine-equals="#pwd"
              data-pristine-equals-message="Passwords don't match"
            />
          </div>

          <div class="form-group" style="text-align: center">
            <button
              type="submit"
              class="btn btn-primary btn-lg"
              style="width: 80%"
            >
              Register
            </button>
          </div>
          <div class="hint-text">
            Already have an account? <a href="/login">Login here</a>
          </div>
          <div class="form-group" id="serverError"></div>
        </form>
      </div>
    </div>
    <script src="./scripts/validation.js" type="text/javascript"></script>
  </body>
</html>